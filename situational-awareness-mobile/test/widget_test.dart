import 'package:flutter_test/flutter_test.dart';

import 'package:situational_awareness_mobile/app.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const SituationalAwarenessMobileApp(), isNotNull);
  });
}
